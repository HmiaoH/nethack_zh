	取决于硬件、操作系统或NetHack的界面，
	某些按键可能无法使用。

	例如，^S和^Q通常用于XON/XOFF流控制，
	这意味着^S暂停输出，随后的^Q恢复暂停的输出。
	在这种情况下，这两个字符都不会在NetHack等待命令按键时
	被传递给它。因此它们不会被用作命令，但如果它们没有
	被传递给NetHack，'whatdoes'可能无法告诉你这一点。

	^M或<return>或<enter>在传递给NetHack处理之前，很可能
	会被转换为^J或<linefeed>或'newline'。
	因此它不被用作命令，而'whatdoes'看起来似乎在报告错误
	的字符，但当它在你输入^M时描述^J，实际上是正常工作的。

	NUL字符在某些键盘上通过^<空格>输入，在另一些键盘上
	通过^@输入，在还有一些键盘上可能根本无法输入，它不被
	用作命令，在到达'whatdoes'之前会被转换为ESC。
	与^M不同，此转换是在NetHack内部执行的。
	但与^M一样，如果你输入NUL后得到关于ESC的反馈，
	这种情况是预期内的。

	ESC本身是^[的同义词，也是另一个奇怪的来源。
	各种功能键，包括光标方向键，可能传输一个
	"转义序列"，即ESC + [ + 其他内容，这会混淆NetHack
	不知道用户想要什么命令，因为ESC会被处理，然后
	后续的内容在NetHack看来就像是——并会被当作——
	用户输入的内容。（如果你按下功能键后出现一个
	显示英雄所穿护甲的菜单，那是因为一个转义序列
	被发送到NetHack，其ESC取消了任何待处理的按键操作，
	其'['被当作显示护甲的命令处理，而"其他内容"
	在你关闭菜单时很可能被当作无效选项静默丢弃。）

	如果你启用了NetHack的'altmeta'选项，意味着当
	<alt>或<option>键在输入其他字符时作为修饰键使用，
	会传输ESC然后传输该字符，这样NetHack应将那个字符
	视为meta字符，那么ESC就会带来更多潜在的混乱。
	在处理两字符序列ESC + 某字符的过程中隐含了一个事实：
	当NetHack看到ESC时，它需要等待另一个字符才能决定
	做什么。所以如果你手动输入ESC，你需要输入第二次，
	否则NetHack会一直等待。
	（然后它会被当作你输入了ESC而非M-ESC来处理。）

	在某些系统上，输入^\会向当前进程发送QUIT信号，
	可能会杀死它，并可能导致它保存core dump。
	它不用于任何NetHack命令，所以不要输入该字符。

	最后一点说明：显示为^x的字符意味着你应该按住
	<control>或<ctrl>键作为修饰键，然后输入'x'。
	控制字符都隐式为大写，但你不需要在输入时按shift键。
	meta字符则相反：它们可以是任意大小写，
	因此你需要同时按下shift和<meta>或<alt>来生成
	大写的meta字符。